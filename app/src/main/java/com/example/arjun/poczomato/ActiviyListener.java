package com.example.arjun.poczomato;

import android.support.v4.app.Fragment;

public interface ActiviyListener {
    void settitle(boolean menuicon,String title);
    void addfragment(Fragment fragment);
}
